{
  "name": "@poolstatis/mcp",
  "version": "0.1.0",
  "description": "Poolstatis MCP stdio runner",
  "license": "PolyForm-Shield-1.0.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "bin": { "poolstatis-mcp": "./dist/cli.js" },
  "exports": { ".": "./dist/cli.js" },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "pnpm --dir ../.. build && rm -rf dist && tsc -p tsconfig.json && mkdir -p dist/core/mcp && cp ../../dist/mcp/server.js ../../dist/mcp/standard.js dist/core/mcp/ && cp ../../dist/schemas.js dist/core/ && cp ../../LICENSE LICENSE",
    "prepack": "pnpm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.25.0"
  }
}
